# Secrets — never commit
.env

# Shared runtime volume (SQLite + exported JSON) — keep the dir, ignore its contents
data/*
!data/.gitkeep

# nginx basic-auth password file (generated at deploy time)
web/.htpasswd

# Local demo output served by the dashboard
web/site/export.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS / editor
.DS_Store
.idea/
.vscode/
