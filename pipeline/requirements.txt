# News Report Agent — pipeline dependencies.
# Pinning is deferred until the open decisions (docs/build-prompt.md) are made.
# Standard library covers IMAP (imaplib), email parsing (email), and SQLite (sqlite3).

pydantic>=2          # structured, validated LLM output
httpx>=0.27          # OpenRouter / embeddings HTTP calls
# tomllib is stdlib on Python 3.11+ (config loading)

# Dev / test
pytest>=8
