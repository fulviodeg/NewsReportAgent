# News Report Agent — pipeline dependencies.
# The container runs Python 3.12, so tomllib is stdlib (no tomli needed there).
# Standard library also covers IMAP (imaplib), email parsing (email), and SQLite (sqlite3).

pydantic>=2          # structured, validated LLM output
httpx>=0.27          # OpenRouter / embeddings HTTP calls
beautifulsoup4>=4.12 # robust HTML parsing of newsletter emails

# Dev / test
pytest>=8
