// Dashboard — loads /export.json and does all filtering and search client-side.
// See docs/v1-architecture.md (Sections 3 and 9, item 7). Stub for the light scaffold.

async function load() {
  // TODO: fetch('/export.json'), render stories, wire up filters + search,
  // auto-refresh when a new export appears.
  throw new Error("not implemented");
}

load();
